sudo: required

language: python
python:
  - "3.8"

install:
  - pip install -r requirements.txt

arch:
  - amd64
  - arm64

os: linux

dist: bionic
addons:
  apt:
    packages:
    - file

matrix:
  include:
  - os: linux
    arch: amd64
  - os: linux
    arch: arm64
    
script:
  - file /bin/bash
  - lscpu
  - free -h
  - python benchtool_runner/main.py config_sample.json 
    