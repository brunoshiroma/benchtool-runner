dist: bionic

language: python
python:
  - "3.8"

install:
  - apt update
  - apt install file
  - pip install -r requirements.txt

arch:
  - amd64
  - arm64

matrix:
  include:
  - os: linux
    arch: amd64
  - os: linux
    arch: arm64
    
script:
  - file /bin/bash
  - lscpu
  - cat /proc/cpuinfo
  - free -h
  - python benchtool_runner/main.py config_sample.json 
    